-- 7 Billion Humans (2059) --
-- 63: Defrag Disordered --

if w == hole or
 w == worker:
	a:
	step nw,w,n,ne,e
	if s == datacube or
	 w == datacube and
	 c == nothing or
	 s == hole:
		drop
		mem4 = nearest worker
		step mem4
		mem1 = nearest datacube
		pickup mem1
	endif
	if ne == hole or
	 sw == hole:
		step mem4
	endif
	jump a
endif


