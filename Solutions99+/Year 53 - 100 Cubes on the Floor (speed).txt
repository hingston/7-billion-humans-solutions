-- 7 Billion Humans (2145) --
-- 53: 100 Cubes on the Floor --

if nw == worker:
	mem1 = set 7
endif
if e == wall and
 s == wall:
	mem1 = set 2
endif
a:
if w != datacube:
	step w
	jump a
endif
if mem1 > 0:
	jump b
endif
c:
if nw == datacube and
 nw != 0:
	mem1 = set nw
	mem1 = calc mem1 - 1
else:
	if sw == datacube and
	 sw != 0:
		mem1 = set sw
		mem1 = calc mem1 + 1
	else:
		jump c
	endif
endif
d:
if w != wall:
	b:
	step w
	pickup c
	write mem1
	drop
	mem1 = calc mem1 + 10
	jump d
endif


