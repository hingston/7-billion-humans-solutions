-- 7 Billion Humans (2214M) --
-- 64: Binary Counter --

a:
if w == worker and
 c != switch or
 se == worker:
	if myitem != datacube:
		pickup c
		step s
	else:
		drop
	endif
	if n == switch or
	 c == button:
		step n
	endif
endif
jump a
