if w == hole:
	takefrom s
	a:
	if myitem == something:
		jump a
	endif
	b:
	takefrom s
endif
if myitem == myitem:
	if myitem == myitem:
		if myitem == myitem:
			if myitem == myitem:
				if myitem == myitem:
					if myitem == myitem:
						if myitem == myitem:
						endif
					endif
				endif
			endif
		endif
	endif
endif
if myitem == something:
	if myitem == myitem:
	endif
	jump b
endif
if e == worker:
	c:
	if myitem == something:
		d:
		e:
		if myitem == myitem:
		endif
		if myitem == something:
			jump e
		endif
		if myitem == myitem:
			if myitem == myitem:
				if myitem == myitem:
					if myitem == myitem:
						if myitem == myitem:
							if myitem == myitem:
							endif
						endif
					endif
				endif
			endif
		endif
		takefrom w
		jump d
	endif
	takefrom nw
	takefrom w
	jump c
endif
f:
if myitem == something:
	g:
	giveto s
	if myitem == myitem:
		if myitem == myitem:
			if myitem == myitem:
				if myitem == myitem:
					if myitem == myitem:
						if myitem == myitem:
							if myitem == myitem:
							endif
						endif
					endif
				endif
			endif
		endif
	endif
	takefrom w
	jump g
endif
takefrom nw
takefrom w
jump f
