if w == hole:
	takefrom s
	a:
	if myitem == something:
		jump a
	endif
	b:
	takefrom s
endif
if myitem == myitem:
	if myitem == myitem:
		if myitem == myitem:
			if myitem == myitem:
				if myitem == myitem:
					if myitem == myitem:
						if myitem == myitem:
						endif
					endif
				endif
			endif
		endif
	endif
endif
if myitem == something:
	if myitem == myitem:
	endif
	jump b
endif
if e == worker:
	c:
	if myitem == something:
		d:
		if myitem == myitem:
		endif
		if myitem == nothing:
			if myitem == myitem:
				if myitem == myitem:
					if myitem == myitem:
						if myitem == myitem:
							if myitem == myitem:
								if myitem == myitem:
								endif
							endif
						endif
					endif
				endif
			endif
			takefrom w
		endif
		jump d
	endif
	takefrom nw
	takefrom w
	jump c
endif
e:
if myitem == something:
	f:
	giveto s
	if myitem == myitem:
		if myitem == myitem:
			if myitem == myitem:
				if myitem == myitem:
					if myitem == myitem:
						if myitem == myitem:
							if myitem == myitem:
							endif
						endif
					endif
				endif
			endif
		endif
	endif
	takefrom w
	jump f
endif
takefrom nw
takefrom w
jump e
