if w == hole:
	takefrom s
	a:
	if myitem == nothing:
		takefrom s
	endif
	jump a
endif
if nw == nw:
	if myitem == myitem:
		if myitem == myitem:
			if myitem == myitem:
				if myitem == myitem:
					if myitem == myitem:
						if myitem == myitem:
							if myitem == myitem:
								if myitem == myitem:
								endif
							endif
						endif
					endif
				endif
			endif
		endif
	endif
endif
if myitem == myitem:
endif
if e == worker:
	b:
	c:
	takefrom w
	if myitem == datacube:
		if myitem == myitem:
			if myitem == myitem:
				if myitem == myitem:
					if myitem == myitem:
						if myitem == myitem:
							if myitem == myitem:
								if myitem == myitem:
								endif
							endif
						endif
					endif
				endif
			endif
		endif
		if myitem == myitem:
			if myitem == myitem:
				if myitem == myitem:
					if myitem == myitem:
						if myitem == myitem:
							if myitem == myitem:
								if myitem == myitem:
								endif
							endif
						endif
					endif
				endif
			endif
		endif
		if myitem == myitem:
			if myitem == myitem:
				if myitem == myitem:
					if myitem == myitem:
						if myitem == myitem:
							if myitem == myitem:
							endif
						endif
					endif
				endif
			endif
		endif
		jump c
	endif
	if nw == nw:
	endif
	jump b
endif
d:
e:
takefrom w
if myitem == datacube:
	giveto s
	if myitem == myitem:
		if myitem == myitem:
			if myitem == myitem:
				if myitem == myitem:
					if myitem == myitem:
						if myitem == myitem:
							if myitem == myitem:
							endif
						endif
					endif
				endif
			endif
		endif
	endif
	jump e
endif
if nw == nw:
endif
jump d
