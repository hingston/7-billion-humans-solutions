-- 7 Billion Humans (2145) --
-- 26: Budget Brigade 2 --

if s == printer:
	a:
	takefrom s
	giveto n
	jump a
endif
if s == worker and
 n != hole and
 ne != worker:
	b:
	if myitem == datacube:
		giveto n
	endif
	jump b
endif
if ne == worker:
	c:
	if myitem >= 50:
		giveto ne
	endif
	if myitem < 50:
		giveto nw
	endif
	jump c
endif
if n == hole and
 s != shredder:
	d:
	if myitem >= 50:
		giveto e
	endif
	if myitem < 50:
		giveto w
	endif
	jump d
endif
if s == shredder:
	e:
	if myitem == datacube:
		giveto s
	endif
	jump e
endif


