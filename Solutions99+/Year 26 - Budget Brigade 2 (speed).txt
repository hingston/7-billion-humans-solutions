if s == printer:
	takefrom s
	a:
	if myitem == nothing:
		takefrom s
	endif
	jump a
endif
if n == worker:
	if myitem == myitem:
		if myitem == myitem:
			if myitem == myitem:
				if myitem == myitem:
					if myitem == myitem:
						if myitem == myitem:
							if myitem == myitem:
								if myitem == myitem:
								endif
							endif
						endif
					endif
				endif
			endif
		endif
	endif
	if nw == worker:
		takefrom nw
		takefrom nw
		takefrom nw
		takefrom nw
		if myitem == myitem:
			if myitem == myitem:
			endif
		endif
		b:
		takefrom s
		if myitem > 49:
			giveto ne
		else:
			giveto nw
		endif
		jump b
	endif
	c:
	takefrom s
	if myitem == something:
		d:
		if myitem == something:
			jump d
		endif
		if myitem == myitem:
			if myitem == myitem:
				if myitem == myitem:
					if myitem == myitem:
						if myitem == myitem:
							if myitem == myitem:
								if myitem == myitem:
								endif
							endif
						endif
					endif
				endif
			endif
		endif
	else:
		takefrom nw
	endif
	jump c
endif
if w == worker and
 e == worker:
	e:
	if myitem == nothing:
		jump e
	endif
	if myitem > 49:
		giveto e
		f:
		if myitem == something:
			giveto e
		endif
		jump f
	endif
	giveto w
	g:
	if myitem == something:
		giveto w
	endif
	jump g
endif
h:
if myitem == something:
	giveto s
endif
jump h
