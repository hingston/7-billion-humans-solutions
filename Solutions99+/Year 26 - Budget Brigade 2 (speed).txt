-- 7 Billion Humans (2059) --
-- 26: Budget Brigade 2 --

if s == printer:
	a:
	takefrom s
	giveto n
	jump a
endif
if w == hole and
 e == hole:
	b:
	if myitem == datacube:
		giveto n
	endif
	jump b
endif
if s == shredder:
	c:
	if myitem == datacube:
		giveto s
	endif
	jump c
endif
if w == worker and
 e == worker:
	d:
	if myitem == datacube:
		if myitem >= 50:
			giveto e
		else:
			giveto w
		endif
	endif
	jump d
endif
